@model IEnumerable<Assessment.Models.Pais>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PaisNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaisImagem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PaisNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaisImagem)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PaisId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PaisId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PaisId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
