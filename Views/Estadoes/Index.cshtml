@model IEnumerable<Assessment.Models.Estado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EstadosNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoImagem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EstadosNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoImagem)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstadosId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EstadosId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EstadosId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
